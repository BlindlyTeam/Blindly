<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ch.epfl.sdp.blindly">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/blindly_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/blindly_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Blindly">
        <activity android:name=".Profile.Profile7"></activity>
        <activity android:name=".Profile.Profile6" />
        <activity android:name=".Profile.Profile5" />
        <activity android:name=".Profile.Profile4_2" />
        <activity android:name=".Profile.Profile4" />
        <activity android:name=".Profile.Profile3" />
        <activity android:name=".Profile.Profile2" />
        <activity android:name=".Profile.Profile1" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".recording.RecordingActivity" />
    </application>

</manifest>